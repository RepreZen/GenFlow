---
swagger: "2.0"
info:
  version: 1.0.0
  title: My API Spec
paths:
  /resourceUrl:
    get:
      parameters: 
        - $ref: "/broken/param/ref"
          
      responses:
        200:
          description: Success 
          schema:
            $ref: "/broken/schema/ref"